{
  age = {
    secrets = {
      # secret1 = { file = ../../secrets/secret1.age; };
      # rakhat-secret = {
      #   file = ../../secrets/rakhat-secret.age;
      #   owner = "rakhat";
      # };
    };
  };
}
